<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.SimpleAuthenticationApp" parent="Base.Theme.SimpleAuthenticationApp">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">@android:color/transparent</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here -->
    </style>
    <style name="Theme.Light" parent="Theme.MaterialComponents.Light">
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>